program StringEquality;
begin
    string left;
    string right;
    left := "alpha" + "beta";
    right := "alphabeta";
    assert(left = right);
    assert(left <> "alpha");
    outtext(left);
    outimage;
    exit(0)
end;
