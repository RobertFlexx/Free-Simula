program NativeSemaphoreSync;
begin
    semaphore gate;

    semaphore_init(gate, 1);
    assert(semaphore_try_wait(gate));
    assert(not semaphore_try_wait(gate));
    semaphore_post(gate);
    semaphore_wait(gate);
    outtext("semaphore sync ok");
    outimage;
    exit(0)
end;
