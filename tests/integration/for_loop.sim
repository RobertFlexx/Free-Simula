program ForLoop;
begin
    integer index;
    integer total;
    total := 0;
    for index := 1 step 1 until 100 do
    begin
        total := total + index;
    end;
    assert(total = 5050);
    exit(0)
end;
