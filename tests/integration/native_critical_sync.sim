program NativeCriticalSync;
begin
    atomic(integer) counter;

    atomic_store(counter, 1);
    synchronized begin
        atomic_fetch_add(counter, 4)
    end;
    assert(atomic_load(counter) = 5);
    outtext("critical sync ok");
    outimage;
    exit(0)
end;
