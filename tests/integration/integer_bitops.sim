program IntegerBitops;
begin
    integer a, b;
    a := 6 and 3;
    b := 4 or 1;
    assert(a = 2);
    assert(b = 5);
    assert((b xor 1) = 4);
    assert((1 shl 5) = 32);
    assert((32 shr 3) = 4);
    outint(a + b);
    outimage;
    exit(0)
end;
