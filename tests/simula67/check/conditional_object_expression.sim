Class Base;
begin
end;
Base Class Derived;
begin
end;
program ConditionalObjectExpression;
begin
    Ref(Base) left;
    Ref(Base) right;
    boolean choose;
    left :- new Base();
    right :- new Derived();
    left :- if choose then left else right;
    left :- left qua Base
end;
