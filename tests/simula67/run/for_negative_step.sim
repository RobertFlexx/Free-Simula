program ForNegativeStep;
begin
    integer item;
    integer total;
    total := 0;
    for item := 5 step -2 until 1 do
        total := total + item;
    outint(total);
    outimage;
    exit(0)
end;
