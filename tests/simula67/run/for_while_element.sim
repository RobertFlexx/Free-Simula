program ForWhileElement;
begin
    integer item;
    integer total;
    item := 0;
    total := 0;
    for item := item + 1 while item < 5 do
        total := total + item;
    outint(total);
    outimage;
    exit(0)
end;
