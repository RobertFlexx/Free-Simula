Class Animal;
begin
end;

Animal Class Dog;
begin
end;

program ObjectRelations;
begin
    Ref(Animal) subject;
    subject :- new Dog();
    if subject in Animal then outtext("A");
    if subject is Dog then outtext("D");
    outimage;
    exit(0)
end;
